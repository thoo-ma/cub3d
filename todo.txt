___________________________
todo

- save as 2nd argument : don't open any window !
- ft_free (into ft_split) en double (free_double_ptr)
- eviter la repetition des casts (maybe une fonction double vector to int vector)
- utiliser les fonction `floor` et `ceil` de math.h
- `char` vs `const char` for map[][]

- fonction polymorphique `new` (ex: new_vector)

t_vector	*new_vector()
{
	t_vector *vector;

	vector = 0;
	vector = malloc(sizeof(t_vector));
	if (!vector)
		exit(1);
	return (vector);
}
___________________________
